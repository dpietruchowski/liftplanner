=== OUTPUT INSTRUCTIONS ===

Step 1: Discuss the plan with the user. Show proposed exercises with sets in the format repsxweightkg (e.g., 12x40kg,12x40kg,12x40kg). Wait for confirmation.

Step 2: After confirmation, generate the full workout plan as valid JSON — a compact array of workouts inside a Markdown ```json code block.

Each workout MUST include a "planned_time" field in ISO 8601 format (e.g., "2026-04-01T10:00:00"). Space workouts across the week.

JSON schema:
[
  {
    "name": "Workout A",
    "planned_time": "2026-04-01T10:00:00",
    "exercises": [
      {
        "name": "Bench Press",
        "description": "Brief technique description.",
        "sets": "12x40kg,12x40kg,12x40kg",
        "youtube_link": "https://www.youtube.com/watch?v=...",
        "rest_seconds": 90
      }
    ]
  }
]

Rules:
- Sets format: repsxweightkg, separated by commas (e.g., 10x60kg,10x60kg,8x65kg)
- Weights in kg (integers), repetitions as integers
- Include youtube_link with a real tutorial video for each exercise
- Include rest_seconds (in seconds) for each exercise
- Output ONLY the JSON array inside ```json — no $schema, no title, no wrapper object
